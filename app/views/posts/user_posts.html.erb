<div class="div-col">
	<% @posts.each do |post|%>
<div class="box" >
		<h2><%= post.title %></h2>
		<br/>
		<p class="p">
		<%= post.text %>
	</p>
	<%= button_to 'Read More', post_path(post), method: "get" %>
</div>
	<% end %>
</div>